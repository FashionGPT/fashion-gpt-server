const express = require('express');
const router = express.Router();
const mongoose = require('mongoose');
const Clothing = mongoose.model('Clothing');

module.exports = router;

// running query on clothing generation by user id
router.get('/get-all', (req, res) => {
    const getClothes = new Clothing();
    Clothing.find().then((result) => {
        console.debug("Retrieved all clothing items generated by the user");
        res.send({result: result}).status(200);
    }).catch((error) => {
        console.error("Unable to retrieve all clothing items", error);
        res.send({message: "failure", reason: error}).status(500);
    });
});